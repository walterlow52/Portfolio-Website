file.create("file.txt")
file.copy("file.txt", "destination_folder")
unlink("file.txt")

#This script is for reuse purposes. 

#To use it, replace the string in the parentheses and run to perform the 
#function. 

#After file is created, copied, or deleted, do not save changes and revert to 
#the previous version of the code. 

#(Remember that R runs only one line of code at a time, so make sure to
#select the line of code you want to run first. If you want to run all lines
#of code, use the mouse and highlight all lines of code before running the
#script.)